top_md_1 = ("""
    <div align="center">
    <div style="font-size: 22px; font-weight: bold;">🎬 AI 字幕剪辑工作台</div>
    <div class="hint">根据声音生成字幕，勾选句子即可批量剪出对应片段，支持说话人识别与字幕烧录</div>
    </div>

    基于本地语音大模型（Paraformer / SenseVoice 系列）实现语音识别、端点检测、标点预测、时间戳预测、说话人区分与热词定制，全程本地离线运行。

    准确识别，自由复制所需段落，或者设置说话人标识，一键裁剪、添加字幕。

    * Step1: 上传视频或音频文件，点击 **<font color="#f7802b">识别</font>** 按钮
    * Step2: 复制识别结果中所需的文字至右上方，或者设置说话人标识，设置偏移与字幕配置（可选）
    * Step3: 点击 **<font color="#f7802b">裁剪</font>** 按钮或 **<font color="#f7802b">裁剪并添加字幕</font>** 按钮获得结果
""")

top_md_2 = ("""
    **LLM 智能剪辑**：输入一句话描述想要的内容，大模型在识别结果中帮你挑选对应片段。
""")

top_md_3 = ("""
    本工作台内置本地语音处理模型链路，可完全离线运行。配套的「分析服务」（端口 61812）提供目录批量分析、台词 JSON 导出、关键词剪辑与剧本匹配剪辑。
""")

top_md_4 = ("""
    **使用须知**：识别与剪辑全程在本机离线完成，素材不会上传。长视频首次分析需要等待模型加载；批量目录分析请使用「分析服务」（61812）。
""")
