D:\xm\aijianjishiping\tools\funclip    检查一下，必须要离线可以运行，再把文案都改一下项目改一下要求看不出来是别人的，再提交GitHub和gitee 对方拉取代码部署到一个文件夹就可以运行，所有依赖和缓存都得在一个文件夹里方便复制文件就可以用，看一下他的文本是不是对应的他那个文本是不是 JSON 格式的？以及他这个裁剪是不是 JSON 文件？

我需要实现以下功能：
1. 目录分析与匹配：我给它一个目录，它会分析目录下的视频，并将你那个文本和视频的时间点对应上。最好生成一个 JSON 文件，相当于每次视频，我就让它分析一遍，在这个视频目录下就有视频和它对应的 JSON 介绍。
2. 关键句裁剪：下一次我剪辑的时候，直接让它把关键的话剪出来就行。
3. 剧本自动匹配剪辑：这个过程做完之后，后期比如我有个小剧本故事，就按照这个故事的每一句话，去我分析过的视频 JSON 里找对应的话（相近或相同的），然后把它们统一全都剪出来。

我要实现这么个功能，但是它原有的功能是正常的，就不要改了。我们是做功能的增加和扩展。